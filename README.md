*Una vez clonado el proyecto ejecutar npm install

21.	Crea un archivo server.ts (o server.js) que muestre el mensaje “Servidor web en ejecución” al ingresar a http://localhost:3000.

    *Localizado en src/models/server.ts
   	*para ejecutar usar npm run dev
23.	Agrega una ruta protegida en Express que devuelva el mensaje “Acceso permitido” solo si el usuario está autenticado (puede simular la autenticación con una variable booleana).
    *Localizado en public/index.html
   	*En la carpeta public ejecutar start index.html o abrir el archivo directamente.
